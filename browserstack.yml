userName: ${BROWSERSTACK_USERNAME}

accessKey: ${BROWSERSTACK_ACCESS_KEY}

framework: playwright

platforms:
  - os: Windows
    osVersion: 11
    browserName: chrome
    browserVersion: latest

  - os: OS X
    osVersion: Ventura
    browserName: safari
    browserVersion: latest

  - os: Windows
    osVersion: 11
    browserName: edge
    browserVersion: latest

parallelsPerPlatform: 2

buildName: Playwright Cucumber

projectName: Automation Framework

testObservability: true

debug: true

networkLogs: true

consoleLogs: info